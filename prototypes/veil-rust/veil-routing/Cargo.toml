[package]
name = "veil-routing"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
veil-dry-run = { path = "../veil-dry-run" }
veil-manifest = { path = "../veil-manifest" }
veil-policy = { path = "../veil-policy" }
